export type NavItem = {
  label: string
  path: string
  moduleKey: string
}

export const navItems: NavItem[] = [
  { label: 'Facturas de venta', path: '/ventas/facturas', moduleKey: 'facturas_venta_33' },
]
